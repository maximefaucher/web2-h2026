# Fonctions utilitaires en JavaScript (version simplifiée)

## Objectif

Cette fiche présente **une façon simple et cohérente** d'écrire des fonctions JavaScript, adaptée aux apprenants débutants.

> 🎯 **Règle du cours** : on utilise **UNE seule manière par défaut** pour déclarer des fonctions utilitaires.

---

## ✅ La façon à privilégier

### Fonction fléchée avec `const`

```js
const nomFonction = (parametres) => {
  // instructions
  return resultat;
};
```

### Exemple simple

```js
const addition = (a, b) => {
  return a + b;
};
```

### Version courte (une seule instruction)

Si la fonction n'a qu'une seule instruction, l'instruction `return` est implicite.

```js
const addition = (a, b) => a + b;
// équivalent à 
// const addition = (a, b) => { return a + b; }
```

---

## ✅ Pourquoi cette forme est idéale pour apprendre

- ✅ Toujours la **même syntaxe**
- ✅ Pas de pièges avec `this`
- ✅ Pas de *hoisting* à comprendre
- ✅ Une fonction = une valeur stockée dans une constante
- ✅ Très répandue dans le JavaScript moderne

> 🧠 **Idée clé** : une fonction est une *recette* stockée dans une variable.

---

## ✅ Exemples de fonctions utilitaires

```js
const estPair = (n) => n % 2 === 0;

const moyenne = (a, b, c) => (a + b + c) / 3;

const convertirEnCelsius = (fahrenheit) => (fahrenheit - 32) * 5 / 9;

const estNoteValide = (note) => note >= 0 && note <= 100;
```

---

## ✅ Bonnes pratiques

- ✔ Utiliser `const`
- ✔ Donner un **nom clair** à la fonction
- ✔ Une fonction = **une seule responsabilité**
- ✔ Fonctions courtes et lisibles

---

## ⚠️ Forme alternative (à connaître, mais pas à mélanger)

```js
function moyenne(a, b, c) {
  return (a + b + c) / 3;
}
```

✅ Acceptable pour :

- les premiers cours
- faire un lien avec Java ou le pseudocode

❌ À éviter : mélanger plusieurs styles dans le même fichier.

---

## ❌ Ce que nous n'utilisons pas pour l'instant

- Expressions de fonction `function () {}`
- `this`
- `arguments`
- IIFE
- Générateurs

> ⏳ Ces notions viendront plus tard dans l'apprentissage.

---

## 📌 Règle à retenir

```js
const fonction = (parametres) => resultat;
// ou
const fonction = (parametres) => {
    // instructions
}
```

👉 **Simple, clair, moderne.**
